services:
  vakilsahab-backend:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    container_name: vakilsahab-backend
    ports:
      - "8000:8000"
    restart: unless-stopped
